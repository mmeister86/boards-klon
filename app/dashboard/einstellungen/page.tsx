import SettingsView from "@/components/settings/settings-view";

// Diese Seite rendert die Einstellungsansicht unter /dashboard/settings
export default function SettingsPage() {
  return <SettingsView />;
}
