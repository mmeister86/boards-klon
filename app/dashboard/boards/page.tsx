import PublishedBoardsView from "@/components/dashboard/published-boards-view";

// Diese Seite rendert die Ansicht der veröffentlichten Boards unter /dashboard/published
export default function PublishedBoardsPage() {
  return <PublishedBoardsView />;
}
